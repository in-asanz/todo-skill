# Execute

## Working contract

- Read the effective files before making changes.
- Keep the node self-contained.
- If the work grows, create a child node instead of overloading this node.
- Re-run `resolve_effective_state.py` after editing local rules, local context, or local roadmap files.
- Re-run `validate_work_package.py` before handoff or closure.

<!-- COMPLEXITY:EXECUTE:START -->
{{complexity_execute}}
<!-- COMPLEXITY:EXECUTE:END -->

## Execution notes

- Replace this section with implementation-specific instructions when the node gets real work.
