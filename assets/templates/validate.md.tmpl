# Validate

## Minimum checks

- Confirm all required node files exist.
- Confirm `meta.yaml` is consistent with the node path and parent.
- Confirm effective files are present and readable.
- Confirm another agent could resume this node from disk only.

<!-- COMPLEXITY:VALIDATE:START -->
{{complexity_validate}}
<!-- COMPLEXITY:VALIDATE:END -->

## Project-specific checks

- Add real tests, commands, or acceptance criteria here when the node targets actual code.
